# Docker Compose Configuration

# Database (PostgreSQL)
DB_NAME=starter
DB_USER=starter_user
DB_PASSWORD=starter_password

# Laravel
# Generate with: docker compose run --rm backend php artisan key:generate
APP_KEY=base64:XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX
APP_DEBUG=true
APP_ENV=local
